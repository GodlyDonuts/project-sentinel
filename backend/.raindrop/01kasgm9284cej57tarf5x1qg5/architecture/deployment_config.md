# Deployment Config

## Environment Variables

- `API_KEY_SECRET`: Secret for API key validation.
- `DATABASE_URL`: Connection string for the SQL database.
- `AI_MODEL_ENDPOINT`: Endpoint for the AI model used in the intelligence service.

## Secrets

- `API_KEY`: The API key for authenticating requests.
- `DB_PASSWORD`: The password for the database.